import { ISignupFormData } from '../utils/interfaces';
// import axios from 'axios';

export async function addToMailingList (formData: ISignupFormData) {
  console.log(formData);
  // return await axios.post('process.env.SERVER_URL', formData);
}